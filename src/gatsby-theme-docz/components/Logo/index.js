import React from 'react';
import logo from './logo.svg';

export const Logo = () => (
  <div style={{ height: 70, paddingTop: 10, overflow: 'hidden' }}>
    <img width={195} src={logo} alt="Revas" />
  </div>
);
